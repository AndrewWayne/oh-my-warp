//! Handlers for `/internal/v1/sessions` (list / register / get / delete).
//!
//! The exact handler signatures are implementation-defined — tests exercise
//! them through the assembled `axum::Router` rather than calling these
//! functions directly. Executor will fill in the bodies and wire them in
//! [`crate::router`].

// Intentionally empty: skeleton only. See crate::router for the assembled
// surface tests rely on.
